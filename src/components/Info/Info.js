import React from "react";
function Info(props) {
  return (
    <div>
      <p className="info">Keep clicking the cards, but never click the same one twice!</p>
    </div>
  );
}

export default Info;
