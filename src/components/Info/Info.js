import React from "react";
function Info(props) {
  return (
    <div>
      <p className="info">Keep clicking the cards, but never click the same one twice! If you clicked every card the game restarts while keeping your score.</p>
    </div>
  );
}

export default Info;
